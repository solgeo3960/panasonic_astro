---
import { faqLinks } from "../data/headerLinks3";
---

<div class="mega-menu mega-menu-sp">
  <ul class="links">
    {
      faqLinks.map((item) => (
        <li>
          <a href={item.url}>{item.text}</a>
        </li>
      ))
    }
  </ul>
</div>
