---
import {
  lineUrls1,
  lineUrls2,
  lineUrls3,
  lineUrls4,
  lineUrls5,
  lineUrls6,
  lineUrls7,
  lineUrls8,
  lineUrls9,
  lineUrls10,
} from "../data/headerLinks1";
---

<div class="mega-menu mega-menu-sp">
  <div class="accordions">
    <div class="accordion">
      <div class="accordion-header js-accordion-header">
        <h3 class="">ブランド表示ガイドラインについて</h3>
      </div>
      <div class="accordion-content">
        <ul>
          {
            lineUrls1.map((item) => (
              <li>
                <a href={item.url}>{item.text}</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <div class="accordion">
      <div class="accordion-header js-accordion-header">
        <h3>ブランド表示の基本原則</h3>
      </div>
      <div class="accordion-content">
        <ul>
          {
            lineUrls2.map((item) => (
              <li>
                <a href={item.url}>{item.text}</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <div class="accordion">
      <div class="accordion-header js-accordion-header">
        <h3>基本要素のルール</h3>
      </div>
      <div class="accordion-content">
        <ul>
          {
            lineUrls2.map((item) => (
              <li>
                <a href={item.url}>{item.text}</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <div class="accordion">
      <div class="accordion-header js-accordion-header">
        <h3>アプリケーションのルール</h3>
      </div>
      <div class="accordion-content">
        <ul>
          {
            lineUrls2.map((item) => (
              <li>
                <a href={item.url}>{item.text}</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
    <div class="normal_link">
      <a href="/guidelines/gl_otherrules.html">その他の表示ルール</a>
    </div>
    <div class="accordion">
      <div class="accordion-header js-accordion-header">
        <h3>ロゴ/ダウンロード</h3>
      </div>
      <div class="accordion-content">
        <ul>
          {
            lineUrls2.map((item) => (
              <li>
                <a href={item.url}>{item.text}</a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</div>
